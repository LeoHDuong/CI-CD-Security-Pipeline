title = "GitLeaks Config"

[extend]
useDefault = true

[allowlist]
description = "Global allowlist"
paths = [
    '''.gitleaks.toml''',
    '''package-lock.json''',
    '''yarn.lock''',
]
regexes = [
    '''(?i)example|placeholder|your[-_]?secret|dummy|fake''',
]